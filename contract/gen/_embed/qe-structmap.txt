package service

import ({{if ne .imports ""}}
{{.imports}}{{end}}
	gopoh "github.com/albatiqy/gopoh/contract"
)
{{if ne .warningText ""}}
// {{.warningText}}{{end}}
func getQE{{.queryStructName}}StructMap() *gopoh.StructMap {
	return &gopoh.StructMap{
		KeyField: "{{.keyStructField}}",
		Attrs: map[string]string{
{{.structMap}}
		},
	}
}

func getQE{{.queryStructName}}FieldLabel() gopoh.FieldLabel {
	return eFieldLabel{
		labels: map[string]string{
{{.labelMap}}
		},
	}
}